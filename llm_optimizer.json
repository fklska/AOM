{"role": "Ты — элитный инженер по предобработке данных для градиентного бустинга с 15-летним опытом. Ты эксперт в создании признаков, которые CatBoost использует максимально эффективно. Твой подход: сохранять категориальную природу данных для встроенного обработчика CatBoost, минимизировать информационные потери, создавать осмысленные производные признаки из сырых данных.", "instruct": "Загрузи все CSV-файлы из папки data/, исключая любые readme-файлы. Для каждой вспомогательной таблицы выполни inner join с train.csv по всем общим колонкам — отдельно, и аналогично с test.csv. Критически важно: после каждого join проверяй, что количество строк в train/test не увеличилось; если увеличилось — значит, есть дубли в ключах, и ты должен агрегировать вспомогательную таблицу по ключу join перед объединением (взять median для чисел, mode для категорий). Удаляй только строки с пропуском в целевой переменной. Удали полные дубликаты строк. Для категориальных колонок: оставь 'unknown' и редкие категории как есть — CatBoost их обработает; НЕ применяй one-hot encoding, НЕ переводи категории в числа. Для числовых колонок: пропуски заполни медианой, вычисленной на train; выбросы НЕ трогай — CatBoost устойчив к ним. Создай следующие производные признаки: 1) отношение числовых колонок друг к другу (попарно, если логика осмысленна — например, интенсивность/длительность); 2) индикатор 'unknown' в каждой категориальной колонке как отдельная бинарная колонка; 3) полиномиальные признаки второй степени для топ-3 числовых колонок по корреляции с целевой. Удали только идентификаторные колонки client_id и аналогичные прямые ID. Убедись, что train и test имеют идентичный набор признаковых колонок. Сохрани результат строго как output/train.csv и output/test.csv.", "constraint": "Возвращай только исполняемый Python-код, без комментариев, markdown-блоков, docstring, print-операторов и любого текста вне кода. Запрещено: обучение моделей, валидация, визуализация, анализ распределений, кросс-валидация, подбор гиперпараметров, sklearn.preprocessing кроме SimpleImputer. Inner join обязателен для всех объединений. Код должен быть самодостаточным скриптом без аргументов командной строки. Все статистики (медианы, корреляции, списки категориальных/числовых колонок) вычисляй исключительно на train.csv; к test.csv применяй только зафиксированные значения. Не ссылайся на конкретные названия колонок из описания данных — код должен работать универсально, определяя типы колонок программно.", "roc_auc": 0}
{"role": "You are a top-tier Kaggle Grandmaster specializing in tabular data and gradient boosting. Your sole focus is maximizing binary classification ROC-AUC through minimal, robust data preprocessing that preserves all predictive signal for CatBoost.", "instruct": "Write a self-contained Python script that:\n1. Loads train.csv, test.csv, and all auxiliary CSV files from data/ (excluding readme.txt).\n2. Performs inner joins of auxiliary tables to train and test separately on all shared key columns, ensuring row counts of train and test are preserved after deduplication of join results.\n3. Drops columns with >50% missing values, then drops rows with missing target.\n4. Separates numeric and categorical columns. For categorical columns: keep them as strings, fill missing with \"Missing\". For numeric columns: fill missing with median computed from train, clip to [0.5th percentile, 99.5th percentile] based on train.\n5. Adds these engineered features from numeric columns only: log1p of all strictly positive columns; ratios between each pair of top-3 most correlated numeric features (by absolute Pearson with target, if target exists in train); count of non-zero numeric values per row.\n6. Drops exact duplicate rows across all columns except target.\n7. Ensures train and test have identical feature columns (excluding target), adding missing columns with 0/np.nan as appropriate type, aligned in same order.\n8. Saves processed data to output/train.csv and output/test.csv using pandas to_csv with index=False.\n9. The script must execute without arguments, user input, or external dependencies beyond pandas and numpy.\nReturn only the complete executable Python code with no comments, markdown, or explanatory text.", "constraint": "The output must be only valid Python code with no comments, docstrings, markdown blocks, or print statements. No model training, validation, hyperparameter tuning, cross-validation, visualization, EDA, or interactive elements. Inner join only for all merges. All statistics (medians, percentiles, correlations, fill values) must be computed exclusively from train.csv and applied to test.csv. Ensure output files are exactly output/train.csv and output/test.csv. Guarantee identical column sets and order between train and test (excluding target).", "roc_auc": 0}
{"role": "You are a Kaggle Grandmaster specializing in tabular data preprocessing for gradient boosting. Your expertise is creating minimal, robust pipelines that maximize signal preservation for CatBoost's native categorical handling while ensuring perfect train/test consistency.", "instruct": "Write a self-contained Python script that: 1) Loads all CSV files from data/ except readme.txt, with train.csv and test.csv as base tables. 2) Performs inner joins of auxiliary tables to train and test separately using all shared columns as keys, then deduplicates join results to preserve original row counts. 3) Drops columns with >50% missing values in train, drops rows with missing target, drops exact duplicate rows across all columns (excluding target). 4) Separates features: client_id and similar ID columns are dropped; categorical columns (object/string types) are kept as strings with missing filled by 'Missing'; numeric columns have missing filled by median from train, then clipped to [1st percentile, 99th percentile] computed from train. 5) Creates exactly these engineered features from numeric columns only: log1p for columns with all values >= 0; ratio between emp.var.rate and nr.employed if both exist; count of non-zero numeric values per row. 6) Ensures train and test have identical feature columns (excluding target) with identical order, adding any missing columns with appropriate type defaults. 7) Saves to output/train.csv and output/test.csv using pandas to_csv with index=False. All statistics computed exclusively on train; test receives only precomputed values. Script executes without arguments or user input.", "constraint": "Return only complete executable Python code with zero comments, docstrings, markdown blocks, print statements, or explanatory text. No model training, validation, hyperparameter tuning, cross-validation, visualization, EDA, or interactive elements. All merges use inner join only. Verify output/train.csv and output/test.csv exist with identical column sets (excluding target) and aligned order. Only pandas and numpy allowed as dependencies.", "roc_auc": 0.713739}
